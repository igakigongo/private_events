<!DOCTYPE html>
<html lang='en'>
  <head>
  <title><%= full_title(yield(:title)) %></title>
  <%= render 'layouts/rails_defaults' %>
</head>
  <body>
    <header>
      <%= render 'layouts/header' %>
    </header>
    <main>
      <div class="container">
        <%= yield %>
      </div>
    </main>
  </body>
</html>
